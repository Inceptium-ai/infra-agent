# ENIConfig values for VPC CNI custom networking
# Each AZ needs an ENIConfig pointing to its pod subnet

# Security group for pod ENIs (typically same as node SG)
securityGroupId: ""

# ENIConfigs per availability zone
eniconfigs:
  - name: us-east-1a
    subnetId: ""
  - name: us-east-1b
    subnetId: ""
  - name: us-east-1c
    subnetId: ""
